# CR 品牌接入状态

## 已完成的第一阶段

- 使用最初认可的实心 CR，不再使用双镂空方向。
- DeerFlow：登录、初始化/强制改密页、展开与折叠聊天侧栏、浏览器图标。
- 登录页采用 360px 单列、居中横排品牌（56px 标志与 36px 名称）和无外框布局，不显示登录副标题；保留密码显隐、恢复入口与保持登录，紧凑选项仍有完整辅助阅读说明。
- PersonalWeb（`/Users/hch/Project/CreeperProject/Creeper`）：导航标志与首页链接、浏览器图标、页面标题。
- 两个项目使用同一份源 PNG；主题仅通过 CSS 调整颜色，不在两张独立生成的图之间切换。源文件仍是不透明光栅图，未宣称完成矢量化。
- 混合模式作用于标志容器，而非被缩放的图片，避免登录页出现黑色方块；强制颜色模式单独覆盖。
- 登录页输入框聚焦改为细边框，无粗外圈；保留按钮、链接和复选框的键盘焦点提示及高对比模式轮廓。样式限定在登录页，不改动共享 UI 组件或其他页面。
- Next Image 负责页面内缩放优化；`/icon` 静态生成 64×64 PNG，避免把整张设计稿当 favicon 下载。
- 个人站笔记的两处既有类型不兼容已修正：React Markdown 改为正确的 `pre > code` 结构，MDXEditor 使用 `defaultCodeBlockLanguage: 'js'`。

## 边界

- 现有账号、密码显隐与恢复入口、聊天、模型、飞书连接、数据库均保留。
- 两边保留独立运行时；通过可选 multi-zone 配置将个人站接到 2026 首页，未启用时保留 DeerFlow 原公共首页。全站历史文案与引擎名称没有全局替换，当前未发布到公网。
- 个人站保留所有旧图片和已有修改。此前双镂空稿与双配色设计稿仅作历史记录。

## 本地入口

- Agent 登录：<http://localhost:2026/login>
- 统一个人网站：<http://localhost:2026/>；独立预览：<http://localhost:3101/about>

## 下一阶段

统一入口与双向硬导航已完成，继续复用 DeerFlow 认证和聊天状态，见
[本地融合配置](../creeper-personal-site.md)。下一阶段是个人站笔记编辑鉴权：
同源路由不等于已获得权限保护，当前仍限定本机使用。不能通过 iframe 或共享
客户端凭据跳过授权。

## 验证

- DeerFlow 定向单元测试 39 项通过。
- 当前登录/初始化/品牌 E2E 共 6 项通过（含日夜、高对比模式、窄屏、鼠标/键盘焦点与 favicon）；公开附件权限的 3 项 E2E 已在初始品牌接入时通过。
- 侧栏 E2E 5 项通过（含新增鼠标与键盘折叠/展开检查）。
- DeerFlow lint + TypeScript 通过；两个前端的品牌接入已通过生产构建，后续简约登录增量通过上述单元与认证 E2E 回归。
- 本地浏览器已核对登录页与 PersonalWeb 导航。未连接模型发起付费请求，未修改任何真实账户或会话数据。
